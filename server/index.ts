import bootstrap from './bootstrap';
import config from './config';
import services from './services';

export default {
  bootstrap,
  config,
  // contentTypes,
  // controllers,
  // destroy,
  // middlewares,
  // policies,
  // register,
  // routes,
  services,
};
