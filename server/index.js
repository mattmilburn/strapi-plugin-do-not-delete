'use strict';

const bootstrap = require( './bootstrap' );
const config = require( './config' );

module.exports = {
  bootstrap,
  config,
  // contentTypes,
  // controllers,
  // destroy() {},
  // middlewares,
  // policies,
  // register() {},
  // routes,
  // services,
};
